services:
  - type: web
    name: purapasion-backend
    runtime: docker
    plan: free
    region: ohio
    # No dockerContext or dockerfilePath needed when Dockerfile is at the root
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
